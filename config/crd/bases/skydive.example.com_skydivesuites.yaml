
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: skydivesuites.skydive.example.com
spec:
  group: skydive.example.com
  names:
    kind: SkydiveSuite
    listKind: SkydiveSuiteList
    plural: skydivesuites
    singular: skydivesuite
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SkydiveSuite is the Schema for the skydivesuites API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SkydiveSuiteSpec defines the desired state of SkydiveSuite
            properties:
              enable:
                properties:
                  agents:
                    default: true
                    type: boolean
                  analyzer:
                    default: true
                    type: boolean
                  flowExporter:
                    default: true
                    type: boolean
                  route:
                    default: false
                    type: boolean
                type: object
              logging:
                properties:
                  level:
                    default: INFO
                    type: string
                type: object
              namespace:
                type: string
            required:
            - enable
            - logging
            type: object
          status:
            description: SkydiveSuiteStatus defines the observed state of SkydiveSuite
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
